ARG BUILD_FROM
FROM ghcr.io/tostmann/zigbee2mqtt-esp32:latest

# The :latest tag is allowed HERE — HA's tag restriction only
# applies to the image: field in config.yaml, not to Dockerfiles.
# Add any small tweaks below if needed; otherwise this is enough.
